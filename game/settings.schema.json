{
  "title": "InkWordle — settings",
  "env": "settings.env",
  "fields": [
    {
      "key": "INKWORDLE_IDLE_MS",
      "label": "Recognize delay (ms)",
      "kind": "select",
      "options": ["", "500", "700", "900", "1200"],
      "help": "How long the pen must pause before your written letters are recognized. Higher = more time to write across cells before it commits. Empty = 900 ms."
    }
  ]
}
